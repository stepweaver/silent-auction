export const metadata = {
  title: 'New Item',
  description: 'Create a new donated item for the Mary Frank Elementary Silent Auction.',
  robots: {
    index: false,
    follow: false,
    noindex: true,
    nofollow: true,
  },
};

export default function VendorNewItemLayout({ children }) {
  return children;
}
