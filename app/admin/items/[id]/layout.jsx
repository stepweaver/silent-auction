export const metadata = {
  title: 'Edit Item',
  description: 'Edit auction item in the Mary Frank Elementary Silent Auction admin dashboard.',
  robots: {
    index: false,
    follow: false,
    noindex: true,
    nofollow: true,
  },
};

export default function EditItemLayout({ children }) {
  return children;
}
